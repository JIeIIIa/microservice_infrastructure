spring:
  application:
    name: library-client
  cloud:
    config:
      uri: http://${CONFIG_SERVER_URI:localhost:9000}
eureka:
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_SERVER_URI:localhost:8888}/eureka